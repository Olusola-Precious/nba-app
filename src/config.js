// Current Year
const CURRENT_YEAR = new Date().getFullYear();
//Url for the app
const URL = "http://localhost:3004";
export {
    // With ES6, if the key and value is the same, no need to state the value
    CURRENT_YEAR,
    URL
}